# "org" ensures this Service is used with the correct Serverless Framework Access Key.
org: goldencodesolutions
# "app" enables Serverless Framework Dashboard features and sharing them with other Services.
app: upwork-job-notifier
# "service" is the name of this project. This will also be added to your AWS resource names.
service: upwork-job-notifier

provider:
  name: aws
  runtime: nodejs20.x

plugins:
  - serverless-plugin-typescript

functions:
  blankFunctionHandler:
    handler: handler.blankFunctionHandler
  #####
  # Temporarily display notifier handler.
  #####
  # jobsNotifierHandler:
  #   handler: handler.jobsNotifierHandler
  #   events:
  #     # Invoke Lambda function every minute
  #     - schedule: rate(30 minutes)